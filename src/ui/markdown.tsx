import { MDXProvider } from '@mdx-js/react';

type Markdown = React.ComponentProps<typeof MDXProvider>['components'];

const markdown: Markdown = {
  /**
   * block-level components
   */
  /**
   * inline-level components
   */
  /**
   * custom components
   */
};
export default markdown;
